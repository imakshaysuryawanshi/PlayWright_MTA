let name = 'Akshay';// in js use '' instead of ""
// also in js we can have ' "" ', but not " ' ' "
let fullName = `my name is ${name} Suyawanshi`;
console.log(fullName);

/*A template literal in JavaScript is a string syntax using backticks (`) 
that allows embedded expressions and multi-line strings easily.
A template literal in JavaScript is a way to create strings using backticks so 
you can easily add variables inside the text.
A template literal in JavaScript is a way to create strings
 using backticks and insert variables or expressions inside the string using ${}.*/

console.log(name);